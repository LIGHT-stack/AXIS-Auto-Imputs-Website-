"use client";

/**
 * Future streaming chat UI — wire to useChat from "ai/react".
 * Legacy keyword chat remains in AxisApp.jsx until migration.
 */
export function ChatWidgetPlaceholder() {
  return null;
}
